# Icons

Place the following PNG icon files in this directory before packaging the extension:

- `icon16.png`  — 16×16 px
- `icon48.png`  — 48×48 px
- `icon128.png` — 128×128 px

These are referenced by `manifest.json` and displayed in the browser toolbar and extension management UI.
